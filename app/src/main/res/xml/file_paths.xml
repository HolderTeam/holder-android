<?xml version="1.0" encoding="utf-8"?>
<!-- Only these cache subdirectories are exposed via FileProvider. See AndroidManifest.xml's
     provider entry, team.holder.android.resource.openResourceExternally, and
     team.holder.android.resource.createCameraCaptureUri. -->
<paths>
    <cache-path name="resource_attachments" path="resource-attachments/" />
    <cache-path name="captures" path="captures/" />
</paths>
